import React from 'react';
import MainNavigator from './MainNavigator'; // Importa o MainNavigator com as rotas

const Index = () => {
  return <MainNavigator />; // Renderiza o MainNavigator que contém a navegação
};

export default Index;
